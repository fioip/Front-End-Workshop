import { Routes, Router, Route, BrowserRouter } from "react-router-dom";

function App() {
  return;
  <>
    <BrowserRouter>
      <Routes>
        <Route path="/" element={}></Route>
        <Route path="/" element={}></Route>
        <Route path="/" element={}></Route>
      </Routes>
    </BrowserRouter>
  </>;
}

export default App;
